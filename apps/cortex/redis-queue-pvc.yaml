apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-queue-pvc
  namespace: cortex
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
